# hallucination-rate

Share of outputs containing a claim not supported by the retrieved context.

Reported by [[eval-harness]]. Measured against what [[context-layer]] actually
returned for the task, so an unsupported claim is attributable rather than a
judgement call.
