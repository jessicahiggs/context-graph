# Generated graphs contain your notes' titles, paths and extracted entities —
# never commit them. This ignores any JSON written at the repo root, which is
# where build_graph.py writes by default (graph.json, sample.json, mine.json...).
/*.json
*.graph.json
__pycache__/
.DS_Store
